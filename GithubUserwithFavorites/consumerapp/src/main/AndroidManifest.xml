<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.githubuserwithfavoritesconsumer">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="com.example.githubuserwithfavorites.READ_DATABASE" />
    <uses-permission android:name="com.example.githubuserwithfavorites.WRITE_DATABASE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubUserwithFavorites">
        <activity android:name="com.example.githubuserwithfavoritesconsumer.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SettingsActivity" />
        <activity android:name=".FavoritesActivity" />
        <activity android:name=".UserDetails" />
    </application>

</manifest>